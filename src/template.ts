// ------------------------------------------------------------------------
// Year YYYY Day DD
// ------------------------------------------------------------------------
import { p, part1, part2 } from '../../util';

function something(input: string[]) {
  return input.length;
}

export function solve(input: string[]): void {
  //// Part 1 ////
  let result1 = something(input);
  part1(result1);

  //// Part 2 ////
  let result2 = something(input);
  part2(result2);
};
